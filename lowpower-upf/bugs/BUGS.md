# Injected power-gating fault campaign — pg_top

> Auto-generated by `bugs/run_bug_demo.sh`. A clean build PASSes; each
> `PG_BUG_*` build must FAIL with a counterexample (the scoreboard has teeth).

- Baseline (clean RTL): **PASS ✅**
- **Result: 3 / 3 injected power bugs caught.**

| Fault (`-D`) | What it breaks | Property violated | Outcome | Counterexample |
|--------------|----------------|-------------------|---------|----------------|
| `PG_BUG_ISO` | isolation cell removed — gated-core X drives straight into the AON domain | A — isolation | ✅ CAUGHT | `[cycle 14] isolation failure: X/Z leaked into AON domain cnt_obs=xxxxxxxx (iso_en=1 pwr_on=0 is_asleep=1)` |
| `PG_BUG_RET` | retention shadow never updated on `save` — state lost across power-down | B — retention | ✅ CAUGHT | `[cycle 26] retention lost: woke with 0, expected preserved value 8` |
| `PG_BUG_SEQ` | power-off BEFORE isolate/save — retention snapshots X, which leaks on wake | A — isolation (mis-sequencing) | ✅ CAUGHT | `[cycle 26] isolation failure: X/Z leaked into AON domain cnt_obs=xxxxxxxx (iso_en=0 pwr_on=1 is_asleep=0)` |

## Properties (enforced every cycle by the scoreboard)
- **A — Isolation:** `cnt_obs` is never X/Z; the always-on domain never
  samples a power-gated core. (4-state Icarus makes this observable.)
- **B — Retention:** the value the AON domain sees right after wake equals
  the value it saw right before sleep — state survived the power cycle.
- **C — Function:** while powered & un-isolated, `cnt_obs` tracks the enable.

Faults are compile-time ``define`` hooks in the RTL (`PG_BUG_*`), so the
clean design is byte-for-byte restored simply by not defining them.
